import styled from 'styled-components'

export const CabecalhoEstilos = styled.header`
  background-color: var(--cor-secundaria);
  color: var(--cor-principal);
  text-align: center;
  padding: 24px 0;
`
